#!/usr/bin/env node

import fs from "node:fs";

const studyPath="diagnostics/v0129-tier-architecture-study.json",historicalPath="diagnostics/v0129-tier-2025-sensitivity.json";
const study=JSON.parse(fs.readFileSync(studyPath,"utf8")),historical=JSON.parse(fs.readFileSync(historicalPath,"utf8"));
study.historicalSensitivity=historical;
const pct=v=>`${(100*v).toFixed(1)}%`,n=(v,d=2)=>v==null?"—":Number(v).toFixed(d),arms=study.mockArms,hist=historical.arms;

const historicalDeltas=Object.fromEntries(Object.entries(hist).map(([arm,x])=>[arm,{fieldRealized:x.noChumps.fieldAverageRealizedOptimalPoints-hist["tier-neutral"].noChumps.fieldAverageRealizedOptimalPoints,noChumpsRealized:x.noChumps.averageRealizedOptimalPoints-hist["tier-neutral"].noChumps.averageRealizedOptimalPoints,gradeCorrelation:x.gradeOutcomeCorrelation-hist["tier-neutral"].gradeOutcomeCorrelation}]));
const mockDeltas=Object.fromEntries(Object.entries(arms).map(([arm,x])=>[arm,{noChumpsGrade:x.noChumps.averageGrade-arms.baseline.noChumps.averageGrade,topThree:x.noChumps.topThreeRate-arms.baseline.noChumps.topThreeRate,firstPlace:x.noChumps.firstPlaceRate-arms.baseline.noChumps.firstPlaceRate}]));
study.comparativeDeltas={mock2026:mockDeltas,historical2025:historicalDeltas};

const proxyRealized=["adp-band-proxy","cliff-only-proxy","compressed-proxy"].map(a=>hist[a].noChumps.averageRealizedOptimalPoints),neutralRealized=hist["tier-neutral"].noChumps.averageRealizedOptimalPoints,bestProxy=Math.max(...proxyRealized),worstProxy=Math.min(...proxyRealized),max2026GradeSwing=Math.max(...Object.values(mockDeltas).map(x=>Math.abs(x.noChumpsGrade)));
let recommendation={code:"G",label:"Insufficient evidence",reason:`Genuine 2025 tiers are unavailable, so the historical arms can measure sensitivity to proxy structure but cannot establish the truth of current boundaries. Cliff-only improved paired 2026 No Chumps grade by ${mockDeltas["cliff-only"].noChumpsGrade.toFixed(3)} and top-three rate by ${(100*mockDeltas["cliff-only"].topThree).toFixed(2)} percentage points, but its 2025 proxy changed No Chumps realized optimal points by ${(hist["cliff-only-proxy"].noChumps.averageRealizedOptimalPoints-neutralRealized).toFixed(1)} and field realized points by ${(hist["cliff-only-proxy"].noChumps.fieldAverageRealizedOptimalPoints-hist["tier-neutral"].noChumps.fieldAverageRealizedOptimalPoints).toFixed(1)}.`};
if(hist["cliff-only-proxy"].noChumps.averageRealizedOptimalPoints>=neutralRealized+5&&mockDeltas["cliff-only"].noChumpsGrade>=-.25&&mockDeltas["cliff-only"].topThree>=0)recommendation={code:"C",label:"Replace static tier urgency with local-cliff logic",reason:"The predeclared cliff arm improved historical proxy realized points by at least five while maintaining 2026 grade and top-three results."};
else if(hist["compressed-proxy"].noChumps.averageRealizedOptimalPoints>=neutralRealized+5&&mockDeltas.compressed.noChumpsGrade>=-.25&&mockDeltas.compressed.topThree>=0)recommendation={code:"E",label:"Reduce tier granularity",reason:"The predeclared compressed arm improved historical proxy realized points by at least five while maintaining 2026 grade and top-three results."};
else if(neutralRealized>=bestProxy+5&&mockDeltas["tier-neutral"].noChumpsGrade>=0&&mockDeltas["tier-neutral"].topThree>=0)recommendation={code:"F",label:"Remove tier-specific strategy effects",reason:"Tier-neutral led all historical proxy arms by at least five realized points and did not reduce paired 2026 grade or top-three frequency."};
study.recommendation={...recommendation,guardrails:{historicalMaterialityPoints:5,maximumAllowed2026GradeLoss:.25,requiresNonnegative2026TopThreeDelta:true},diagnosticContext:{neutral2025NoChumpsRealized:neutralRealized,bestProxy2025NoChumpsRealized:bestProxy,worstProxy2025NoChumpsRealized:worstProxy,maxAbsolute2026GradeSwing:max2026GradeSwing}};

function boundaryTable(rows){return ["| Pos | Boundary | Last upper | First lower | ADP gap | Intrinsic gap | STR gap | Flags |","|---|---:|---|---|---:|---:|---:|---|",...rows.map(x=>`| ${x.position} | ${x.upperTier}→${x.lowerTier} | ${x.lastUpperPlayer} | ${x.firstLowerPlayer} | ${n(x.adpGap)} | ${n(x.intrinsicGap)} | ${n(x.draftStrengthGap)} | ${x.flags||"—"} |`)]}
function armTable(){return["| Arm | NC grade/rank | First/top 3 | QB/TE timing | 2QB / 2TE | RB R4/R6/R8 | WR R6/R8 | Diff picks | Legal |","|---|---:|---:|---:|---:|---:|---:|---:|---:|",...Object.entries(arms).map(([name,x])=>`| ${name} | ${n(x.noChumps.averageGrade)} / ${n(x.noChumps.averageRank)} | ${pct(x.noChumps.firstPlaceRate)} / ${pct(x.noChumps.topThreeRate)} | ${n(x.timing.firstQbRound)} / ${n(x.timing.firstTeRound)} | ${pct(x.timing.secondQbFrequency)} / ${pct(x.timing.secondTeFrequency)} | ${n(x.rbAfterR4)} / ${n(x.rbAfterR6)} / ${n(x.rbAfterR8)} | ${n(x.wrAfterR6)} / ${n(x.wrAfterR8)} | ${pct(x.pairedDifference.differentPickRate)} | ${pct(x.starterLegality)} |`)]}
function historicalTable(){return["| Arm | NC realized | Field realized | Realized first/top 3 | Grade→realized r | Grade-decile gap | 2QB / 2TE | RB / WR | Roster diff |","|---|---:|---:|---:|---:|---:|---:|---:|---:|",...Object.entries(hist).map(([name,x])=>`| ${name} | ${n(x.noChumps.averageRealizedOptimalPoints,1)} | ${n(x.noChumps.fieldAverageRealizedOptimalPoints,1)} | ${pct(x.noChumps.realizedFirstPlaceFrequency)} / ${pct(x.noChumps.realizedTop3Frequency)} | ${n(x.gradeOutcomeCorrelation,3)} | ${n(x.topVsBottom.difference,1)} | ${pct(x.construction.twoQbFrequency)} / ${pct(x.construction.twoTeFrequency)} | ${n(x.construction.averageCounts.RB)} / ${n(x.construction.averageCounts.WR)} | ${pct(x.pairedNoChumpsRosterDifferenceRate)} |`)]}
function extremes(key,title){const rows=study.localCliffAudit.extremes[key]||[];return["### "+title,"",rows.length?["| Pos | Player | Tier | Remain | Static urgency | Local drop | Demand | Denial |","|---|---|---:|---:|---:|---:|---:|---:|",...rows.slice(0,8).map(r=>`| ${r.position} | ${r.name} | ${r.tier} | ${r.remainingInTier} | ${n(r.tierUrgency)} | ${n(r.localBaseStrengthDrop)} | ${n(r.expectedOpponentDemand)} | ${n(r.denialSignal)} |`)].join("\n"):"No qualifying initial-state cases.",""]}
function scenarioLines(position){return study.scenarios.filter(s=>s.position===position).map(s=>{const b=s.arms.baseline[0],z=s.arms["tier-neutral"][0],c=s.arms["cliff-only"][0],m=s.arms.compressed[0];return`- **${s.name}** — baseline ${b?.name||"none"} (STR ${n(b?.draftStrength)}, SURV ${n(b?.survival)}, roster ${n(b?.rosterUtility)}, urgency ${n(b?.tierUrgency)}); neutral ${z?.name||"none"} (${n(z?.draftStrength)}); cliff ${c?.name||"none"} (${n(c?.draftStrength)}); compressed ${m?.name||"none"} (${n(m?.draftStrength)}).`})}

const suspicious=study.boundaries.filter(x=>(x.flags||"").split("|").some(flag=>flag&&!flag.startsWith("SUPPORTED_BY_"))),teBoard=study.tierSummaries.filter(x=>x.position==="TE"),qbBoard=study.tierSummaries.filter(x=>x.position==="QB");
const lines=[
"# v0.12.9 tier architecture study","","> Analysis only. Production recommendation logic, coefficients, player pool, owner profiles, grading, and PWA/update files were not changed.","",
"## Executive conclusion","",`**${recommendation.code}. ${recommendation.label}.** ${recommendation.reason}`,"",`Across 2026 arms, the largest absolute No Chumps grade swing was ${n(max2026GradeSwing,3)} points. Historical No Chumps realized optimal points ranged from ${n(worstProxy,1)} to ${n(Math.max(neutralRealized,bestProxy),1)}, but all structured 2025 tiers are synthetic proxies because genuine archived tiers do not exist.`,"",
"## 1. Exact production tier architecture","",`Source: \`player.tier\` in the embedded \`DEFAULT_MASTER_POOL\`; all populated 2026 rows report \`${study.sourceTrace.tierAssignment.observedMethod.join(", ")}\`. Tiering is position-specific. The artifact does not retain the upstream builder threshold implementation, so the exact historical assignment thresholds cannot be recovered beyond the frozen labels and method provenance.`,"",...Object.entries(study.sourceTrace.functions).map(([name,text])=>`- \`${name}\`: ${text}`),"","Formula dependency map:","","```text","player.tier","  ├─> positionalScarcityScore (TE only)","  │     └─> rosterUtility","  │            └─> Draft Strength","  └─> tierInfo: same-tier remaining + ownerHazard","         └─> tier urgency","SURV ─────────┴─> urgency = 70% (100-SURV) + 30% tier urgency","                        └─> 11% of Draft Strength","tier boundary ─> tierCliffDiagnostic ─> exported diagnostic only (zero strategy contribution)","```","",...Object.entries(study.sourceTrace.effects).map(([k,v])=>`- ${k}: ${v}`),"",
"## 2. Complete current 2026 tier board","",...study.tierSummaries.map(t=>`- **${t.position} ${t.tier==null?"Unassigned":`Tier ${t.tier}`} (${t.playerCount})** — ${t.players}`),"","The complete player-level board, including assigned and unassigned players, ranks, ADP, intrinsic, situation, projection, role/depth, RB contingent score, STR, SURV, and urgency, is in the CSV and JSON.","",
"## 3. Tier granularity audit","","| Position | Tiers | Assigned / all | Avg/tier | Median | Min–max | Mean ADP spread | Mean intrinsic spread | Compressed tiers |","|---|---:|---:|---:|---:|---:|---:|---:|---:|",...Object.entries(study.granularity).map(([p,g])=>`| ${p} | ${g.tiers} | ${g.assignedPlayers} / ${g.players} | ${n(g.averagePlayersPerTier)} | ${n(g.medianPlayersPerTier,1)} | ${g.smallestTier}–${g.largestTier} | ${n(g.averageWithinTierAdpSpread)} | ${n(g.averageWithinTierIntrinsicSpread)} | ${g.compressedTierCount} |`),"",`Granularity is position-dependent: assigned QB and TE tiers average ${n(study.granularity.QB.averagePlayersPerTier)} and ${n(study.granularity.TE.averagePlayersPerTier)} players, versus ${n(study.granularity.RB.averagePlayersPerTier)} RB and ${n(study.granularity.WR.averagePlayersPerTier)} WR. The strict compression rule merged only ${study.granularity.QB.tiers-study.granularity.QB.compressedTierCount} QB, ${study.granularity.RB.tiers-study.granularity.RB.compressedTierCount} RB, ${study.granularity.WR.tiers-study.granularity.WR.compressedTierCount} WR, and ${study.granularity.TE.tiers-study.granularity.TE.compressedTierCount} TE boundaries.`,"",`${suspicious.length} of ${study.boundaries.length} adjacent boundaries triggered at least one predeclared suspicion flag; supported major ADP cliffs alone are not counted as suspicious.`,"",...boundaryTable(suspicious),"",
"## 4. Static urgency versus local cliffs","",...extremes("staticHighLocalSmall","A. Static urgency high, local cliff small"),...extremes("staticLowLocalLarge","B. Static urgency low, local cliff large"),...extremes("nearLastEquivalent","C. Near-last in tier, next tier nearly equivalent"),...extremes("manyRemainSevere","D. Many remain, next-tier cliff severe"),...extremes("demandDangerMildCliff","E. Demand threatens exhaustion despite mild cliff"),...extremes("lowDemandBoundary","F. Low demand despite apparent boundary"),
"## 5. Elite TE audit","",...teBoard.map(t=>`- ${t.tier==null?"Unassigned":`Tier ${t.tier}`} (${t.playerCount}): ${t.players}`),"",...scenarioLines("TE"),"",`Paired timing: baseline first TE ${n(arms.baseline.timing.firstTeRound)}, second-TE frequency ${pct(arms.baseline.timing.secondTeFrequency)}; neutral ${n(arms["tier-neutral"].timing.firstTeRound)} / ${pct(arms["tier-neutral"].timing.secondTeFrequency)}; cliff ${n(arms["cliff-only"].timing.firstTeRound)} / ${pct(arms["cliff-only"].timing.secondTeFrequency)}; compressed ${n(arms.compressed.timing.firstTeRound)} / ${pct(arms.compressed.timing.secondTeFrequency)}. Removing static tiers deferred TE1 by ${n(arms["tier-neutral"].timing.firstTeRound-arms.baseline.timing.firstTeRound)} rounds and reduced 2TE frequency by ${n(100*(arms.baseline.timing.secondTeFrequency-arms["tier-neutral"].timing.secondTeFrequency),2)} percentage points. That shows current tiers reward TE scarcity, but the mixed historical proxy result does not prove the amount is correct.`,"",
"## 6. QB tier audit","",...qbBoard.map(t=>`- ${t.tier==null?"Unassigned":`Tier ${t.tier}`} (${t.playerCount}): ${t.players}`),"",...scenarioLines("QB"),"",`Paired timing: baseline first QB ${n(arms.baseline.timing.firstQbRound)}, 2QB ${pct(arms.baseline.timing.secondQbFrequency)}; neutral ${n(arms["tier-neutral"].timing.firstQbRound)} / ${pct(arms["tier-neutral"].timing.secondQbFrequency)}; cliff ${n(arms["cliff-only"].timing.firstQbRound)} / ${pct(arms["cliff-only"].timing.secondQbFrequency)}; compressed ${n(arms.compressed.timing.firstQbRound)} / ${pct(arms.compressed.timing.secondQbFrequency)}. First-QB timing moved by at most ${n(Math.max(...[arms["tier-neutral"],arms["cliff-only"],arms.compressed].map(x=>Math.abs(x.timing.firstQbRound-arms.baseline.timing.firstQbRound))))} rounds and 2QB frequency by at most ${n(100*Math.max(...[arms["tier-neutral"],arms["cliff-only"],arms.compressed].map(x=>Math.abs(x.timing.secondQbFrequency-arms.baseline.timing.secondQbFrequency))),2)} percentage points. Different-bye coverage enters through independent \`byeImpact\`, not tiers; denial remains diagnostic-only.`,"",
"## 7. RB and WR audit","",`Baseline averaged ${n(arms.baseline.rbAfterR4)}/${n(arms.baseline.rbAfterR6)}/${n(arms.baseline.rbAfterR8)} RBs through R4/R6/R8 and ${n(arms.baseline.wrAfterR6)}/${n(arms.baseline.wrAfterR8)} WRs through R6/R8. Tier-neutral: ${n(arms["tier-neutral"].rbAfterR4)}/${n(arms["tier-neutral"].rbAfterR6)}/${n(arms["tier-neutral"].rbAfterR8)} RB and ${n(arms["tier-neutral"].wrAfterR6)}/${n(arms["tier-neutral"].wrAfterR8)} WR. Cliff-only: ${n(arms["cliff-only"].rbAfterR4)}/${n(arms["cliff-only"].rbAfterR6)}/${n(arms["cliff-only"].rbAfterR8)} RB and ${n(arms["cliff-only"].wrAfterR6)}/${n(arms["cliff-only"].wrAfterR8)} WR.`,"",`The RB tier experiment leaves production contingent-RB logic untouched, so any arm difference isolates tier behavior rather than contingent-upside retuning. WR effects arise only from tier urgency; WR has no TE-like direct tier-number scarcity bonus.`,"",
"## 8–9. Paired 2026 mock experiment","",`Each arm completed ${study.metadata.mockDraftsPerArm.toLocaleString()} mocks using identical seeds ${study.metadata.seeds.first}–${study.metadata.seeds.last}, randomized cards/rooms, current profiles, the current 331-player pool, and the frozen grading system.`,"",...armTable(),"",`Selected-value averages, label distributions, final positional counts, K/DST timing, bye loads, per-round/position changes, and boundary concentration are fully retained in JSON and flattened into \`v0129-tier-mock-arms.csv\`.`,"",
"## 10. 2025 historical sensitivity","",`This is **sensitivity analysis, not proof of historical tier truth**. Each arm completed ${historical.metadata.draftsPerArm.toLocaleString()} paired mocks with identical seeds. Outcomes are scored only after each 170-pick draft completes.`,"",...historicalTable(),"",`Primary historical question: relative to no tiers, the best proxy changed No Chumps realized optimal points by ${n(bestProxy-neutralRealized,1)}; the worst changed them by ${n(worstProxy-neutralRealized,1)}. These deltas cannot validate the current 2026 boundaries because the 2025 bands are synthetic.`,"",
"## 11. Tier-boundary case studies","","| Case | Player | Tier→next | Baseline STR | No-tier STR | Cliff STR | SURV | Demand | Remain | Estimated drop |","|---|---|---:|---:|---:|---:|---:|---:|---:|---:|",...study.caseStudies.map(c=>`| ${c.label} | ${c.player} | ${c.currentTier}→${c.nextTier} | ${n(c.currentDraftStrength)} | ${n(c.noTierDraftStrength)} | ${n(c.cliffOnlyDraftStrength)} | ${n(c.survival)} | ${n(c.expectedOpponentDemand)} | ${c.remainingInTier} | ${n(c.estimatedDrop)} |`),"",`Representative-state selection changes are listed in the TE/QB sections and all six candidates per state are stored in JSON.`,"",
"## 12. Tier value versus tier label","",`Across ${study.tierValueDiagnostics.candidateRows.toLocaleString()} candidate-choice observations, point-biserial correlations with selection were: tier number ${n(study.tierValueDiagnostics.selectionCorrelations.tierNumber,3)}, static urgency ${n(study.tierValueDiagnostics.selectionCorrelations.tierUrgency,3)}, local cliff ${n(study.tierValueDiagnostics.selectionCorrelations.localCliffDrop,3)}, depletion probability ${n(study.tierValueDiagnostics.selectionCorrelations.depletionProbability,3)}, and Draft Strength ${n(study.tierValueDiagnostics.selectionCorrelations.draftStrength,3)}. These diagnose redundancy in selection behavior; they are not realized-outcome coefficients.`,"",
"## 13. Recommendation","",`**${recommendation.code}. ${recommendation.label}.**`,"",recommendation.reason,"",`A production change should wait for archived point-in-time tiers or forward holdout realized outcomes. The current evidence supports shadow diagnostics and boundary review, but not coefficient retuning or production replacement.`,"",
"## Reproducibility and integrity","",`- Production commit: \`${study.metadata.productionCommit}\`.`,`- 2026 runner: \`diagnostics/v0129-tier-architecture-study.mjs\`.`,`- 2025 runner: \`diagnostics/v0129-tier-2025-sensitivity.mjs\`.`,`- Production hashes unchanged in both studies: **${study.productionIntegrity.unchanged&&JSON.stringify(historical.metadata.productionHashesBefore)===JSON.stringify(historical.metadata.productionHashesAfter)?"PASS":"FAIL"}**.`,`- No production player, profile, grade, recommendation, bye, contingent-RB, adaptation, export, or PWA artifact was written.`
];
fs.writeFileSync(studyPath,JSON.stringify(study,null,2)+"\n");
fs.writeFileSync("diagnostics/v0129-tier-architecture-study.md",lines.join("\n")+"\n");
console.log(JSON.stringify({report:"diagnostics/v0129-tier-architecture-study.md",json:studyPath,recommendation:study.recommendation,comparativeDeltas:study.comparativeDeltas},null,2));
